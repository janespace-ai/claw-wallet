{
  "_comment_relayUrl": "Relay server WebSocket URL (env: CLAW_DESKTOP_RELAY_URL)",
  "relayUrl": "ws://localhost:8080",

  "_comment_ipChangePolicy": "IP change policy: \"block\" rejects, \"warn\" alerts, \"allow\" ignores",
  "ipChangePolicy": "allow",

  "_comment_lockMode": "Lock mode: \"convenience\" allows biometric, \"strict\" enforces password + idle timeout",
  "lockMode": "convenience",

  "_comment_chains": "Web3 network configuration - Local development environment uses localhost nodes",
  "chains": {
    "ethereum": {
      "_comment": "Local Ethereum node (Hardhat/Anvil) simulating mainnet",
      "_comment_setup": "Start with: npx hardhat node --chain-id 1 --port 8545",
      "rpcUrl": "http://localhost:8545"
    },
    "base": {
      "_comment": "Local Base node (Hardhat/Anvil) simulating mainnet",
      "_comment_setup": "Start with: npx hardhat node --chain-id 8453 --port 8546",
      "rpcUrl": "http://localhost:8546"
    }
  },

  "_comment_relay": "Relay connection tuning parameters",
  "relay": {
    "_comment_reconnectBaseMs": "Base delay in ms before first reconnect attempt",
    "reconnectBaseMs": 500,
    "_comment_reconnectMaxMs": "Maximum reconnect delay in ms, caps exponential backoff",
    "reconnectMaxMs": 5000
  },

  "_comment_signing": "Signing engine auto-approval budget parameters - higher limits for local testing",
  "signing": {
    "_comment_dailyLimitUsd": "Maximum total USD value of auto-approved transactions per day",
    "dailyLimitUsd": 10000,
    "_comment_perTxLimitUsd": "Maximum USD value per single auto-approved transaction",
    "perTxLimitUsd": 1000,
    "_comment_tokenWhitelist": "Token symbols allowed for auto-approval",
    "tokenWhitelist": ["ETH", "USDC", "USDT", "DAI"],
    "_comment_autoApproveWithinBudget": "When true, transactions within daily/per-tx limits may sign without a desktop prompt. When false, every chain tx requires approval.",
    "autoApproveWithinBudget": true
  },

  "_comment_lock": "Lock manager parameters",
  "lock": {
    "_comment_strictIdleTimeoutMs": "Idle timeout in ms before auto-lock in strict mode (disabled for local dev)",
    "strictIdleTimeoutMs": 999999999
  },

  "_comment_security": "Security monitor parameters",
  "security": {
    "_comment_maxEvents": "Maximum number of security events to retain in memory",
    "maxEvents": 100
  }
}
